/**
 * Supabase has been removed.
 * Auth and user progress now use localStorage only.
 * This stub prevents leftover imports from breaking the build.
 */
export const supabase = null as unknown as never;
